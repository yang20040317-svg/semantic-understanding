---
name: semantic-understanding
description: 读取并持续学习用户个人的表达习惯——把「你的原话 → 你的真意」沉淀成可复用规则。**当用户给出简短、省略、模糊、指代不明或字面易误读的指令时，动手前先取用本档案**；也用于设计/工程/交付类任务开始前对齐口径。当用户抱怨「你又理解错了/我说的是 X 不是 Y」或要求「记一下/以后我说 X 就是 Y」时，用它记录纠偏并入库。
agent_created: true
version: 1.0.0
---

# Semantic Understanding · 语义理解层

> 提示词优化改的是**「AI 怎么说」**；这一层改的是**「AI 怎么听」**。
> 沉淀的不是通用技巧，是**这个人**的表意习惯。

## 定位：与相邻能力的分工

| 能力 | 存什么 | 回答什么问题 |
|------|--------|--------------|
| `starfounder-experience-alchemy` | 做事的坑、流程、checklist | "这件事怎么做才不踩坑" |
| **本技能** | 说话的习惯、误解映射 | **"他这句话到底什么意思"** |
| 提示词优化 | 指令措辞 | "怎么把话写清楚" |

三者正交。本技能**不修改** alchemy 或任何其他技能的文件，全部数据位于自身目录内。

## 常驻机制：它怎么进入上下文（2026-09-25 实装）

技能本体是**懒加载**的 —— 宿主每会话只注入 name + description，`SKILL.md` 正文要触发才读。
所以「装了」不等于「在生效」。真正让它每轮生效的是下面三层：

| 层 | 位置 | 性质 |
|---|---|---|
| **hook 自动注入** | `~/.workbuddy/settings.json` 的 `hooks.UserPromptSubmit` | 硬机制，无需人介入 |
| **user memory 硬指令** | `~/.workbuddy/MEMORY.md` 顶部 | 每会话必注入，兜底 |
| 显式调用 | `@skill:semantic-understanding` 或直接跑 `su.py` | 手动，最可控 |

### hook 行为（`hook.py`）

用户每发一句话，宿主调用 `hook.py`，它把命中的规则以 `additionalContext` 注入：

- **常驻规则**：每会话首批注入一次（靠 `session_id` 状态记录）
- **长尾命中**：按当轮这句话现算，命中即注入
- **两重去重**：① 常驻规则有**与 session 无关的 10 分钟窗口保险** —— 万一张口每轮都换
  `session_id`，也不至于每轮重复塞 9 条规则；② 同一组长尾连续命中不重复注入。
- **绝不阻塞**：任何异常都静默 `exit 0` 零输出。hook 坏掉最多是没注入，绝不能让你发不出话。

### 自检（必跑）

```bash
python "$SU" doctor
```

防的是**静默失效**：hook 命令里写的是解释器**绝对路径**，WorkBuddy 升级 managed Python、
版本目录改名后，hook 会无声无息地不再注入 —— 表现只是「AI 怎么又听不懂了」，
没人会想到是配置断了。`doctor` 把它变成可检查的，并直接给出修复要用的解释器路径。

**已知坑**：`~/.workbuddy/settings.json` 里的 hook 命令**不要写成 `bash script.sh`**。
本机 `bash` 这个名字被解析到 `C:\Windows\System32\bash.exe`（WSL 启动器），
会被安全策略拦截（`wsl.exe` 在黑名单里）。直接写 `"<python.exe 绝对路径>" "<hook.py 绝对路径>"`。
同理，hook 脚本**不要依赖 coreutils**（本机 Git Bash 缺 `grep/sed/head/dirname`）。

## 三级行为协议（宿主必须遵守）

### L0 · 静默应用

当前用户的表述**命中常驻 core rule**（`profile/expression-profile.md`，>=2 份证据支撑）：

- **直接按翻译后的语义执行**，一句话都不解释。
- 不要复述："根据我的理解你的意思是……" —— 已经懂了还复述是噪音。
- 例外：见下面 L1 的不可逆场景名单。

### L1 · 复述确认

命中以下任一条件，**动手前输出一行**，只有一行：

```
我理解为：<用你的话重述意图>  ← 如果不是，打断我。
```

触发条件：

1. **无命中**，或只命中长尾单证据案例（`cases/` 里 hits=1，未经二次验证）；
2. 用户这句话**跳跃、省略、指代不明**，补全后仍有两种以上合理解读；
3. **不可逆 / 高成本动作**：写文件、删东西、git push、大范围重构、对外发送、花钱。
   —— 这类场景**即使 L0 命中也要复述**，铁律。

### L2 · 纠偏留档

出现以下任一信号，**立刻 capture 候选**（不等会话结束）：

- 用户说："不对 / 不是这个意思 / 我说的是 X / 你又理解错了 / 重来 / 谁让你……"
- 用户把你刚做完的东西推翻重做
- 用户**第二次**强调同一个点（说明第一次没被听进去——这种最该记）
- 用户主动说："记一下 / 以后我说 X 就是 Y / 记住这一点"

capture 时的**质量门槛**（不满足就别记，记了也是噪）：

- `surface` 尽量是**用户原文**，不是你的转述
- `intended` 要写清**他到底要什么**，不是"他不要什么"
- `rule` 要**可泛化**：同一句话在不同场景下还应成立。只对这一次有效的东西写成 case 但不提炼 rule。

## 混合采集：会话收尾确认

本次会话**如果产生了候选**，收尾时给一张清单，**不要超过 5 条**：

```bash
python "<skill-dir>/su.py" pending
```

原文呈给用户："这次有 N 条表达习惯候选，留哪些？（全留 / 留 1 3 / 都扔）"

用户挑完：

```bash
python "<skill-dir>/su.py" confirm c-20260925-001
python "<skill-dir>/su.py" reject  c-20260925-002
```

**候选不经确认不进案例库** —— 这是防噪的唯一闸门，不许跳过。

## 可视化：默认产物，不是可选项

**任何写操作成功后自动重绘面板**（`auto_viz: true`），不需要用户开口要、也不需要记得跑命令。
交付本技能相关产物时，**默认随附面板**，不得只给一堆 JSON / markdown。

```bash
python "$SU" viz           # 手动重绘
python "$SU" viz --open    # 重绘并用浏览器打开
```

面板：`viz/index.html` —— 自包含单文件，离线可开，零 CDN 依赖。

四块内容，全部承载信息而非装饰：

| 区块 | 回答什么 | 为什么值得画 |
|------|----------|--------------|
| 指标条 | 多少案例 / 常驻 / 长尾 / 待确认 | 一眼看清库的健康度，pending 有货时会变琥珀色告警 |
| **检索试算器** | 我现在这句话会撞上哪条习惯 | 交互式验证召回，可当场调 `rule` 措辞 |
| **语义错位对照表** | 字面误读 → 真实意图 | 这是全部资产本身，支持按 domain 筛选 |
| **证据阶梯** | 哪条规则快升级了 | 红线是 `promote_threshold`，差的份数直接可见 |

### 判据：三层提纯 + 两道门控（2026-09-25 重写）

判据**不是**朴素 n-gram 相似度 —— 实测过它会坏成什么样：

| 病灶 | 现象 | 成因 |
|------|------|------|
| 虚字撞车 | 「明天要不要带伞」得 **0.6433**，撞上「要不要登记」 | 中文功能字组 `不要`/`要不` 被当成了相似度信号 |
| 跨语言残词 | 「token」撞上「documentary」 | 英文 2-gram `to`/`en` 毫无信息量 |
| 短句失明 | 「改改」得 0.00，而它最该命中「短指令」规则 | 短句与任何长文本都零字面重合 |

**关键教训**：假阳性（0.6433）分数**高于**真阳性（0.5975）。所以单一阈值永远治不了它 ——
门槛只能切掉低分项，切不掉排在最前面的错误。必须换判据，不能调门槛。

三层提纯（`su.py` 的 `feats()`）：

1. **分段取特征** —— 汉字段取 2/3/4-gram；拉丁段取整词 + 4-gram（**不切 2/3-gram**）
2. **丢中文功能字** —— 任一 n-gram 若全由 `的地得了是在我你…` 构成，直接丢弃
3. **按长度加权** —— 2/3/4-gram 权重 `0.45 / 0.85 / 1.15`；**零重合直接返回 0**（不再白送 `hits*0.01`）

两道门控（`su.py` 的 `select_hits()`，三处调用必须共用它）：

4. **领先判据** —— 其余条目须达 `top1 × tail_ratio`（默认 0.6）才一起注入，滤掉跟在真命中后面的噪音
5. **短句兜底** —— 汉字段 ≤ `short_prompt_len`（默认 6）且无命中时，兜到 `fallback_case_id`。
   极短指令靠字面永远判不出语义，只能用结构特征兜。

校准后实测：真阳性命中落在 **0.33 ~ 0.95**，噪音上限 **0.05**，`min_score=0.22` 落在安全间隔中部。

**已知边界（诚实记录）**：字面判据分不清同词异义。查询里的「技能」会撞上案例里的「技能」
（所指不同），拿到约 0.34 分。根治需 IDF 或语义模型，超出「零依赖标准库」的约束，暂不处理。

### 硬约定：面板上的数字必须能在终端复现

试算器的分数由 JS 重算，与 `su.py recall` 必须**逐位一致**。JS 侧完整复刻了 `feats()` 与
`score()`，门控（硬门槛 → 领先判据 → 短句兜底）逐条对齐。

**改 `su.py` 的判据，必须同步改 `viz.py` 里的 JS，然后跑对拍：**

```bash
python "<skill-dir>/parity.py"                 # 全库案例 × 内置 8 探针，容差 1e-4
python "<skill-dir>/parity.py" "自定义说法"
```

对拍比对的是**全库逐条分数**，不是 top5 名次 —— 名次一致可能是巧合，逐条一致才证明判据等价。
内置探针含三条历史病灶，任何一条回归都要警觉。任由它漂移，仪表盘就变成了装饰。

### UI 红线（限su 一次即可，后续改动沿用）

分区用 hairline + 留白，**不用卡片容器**；色只承担语义（琥珀=字面误读并带删除线，
青绿=修正后的实指）；深色底；数字一律 `tabular-nums` 严格成列；
字号极端跳档（64/28/15/11/9）而非等差递减；正文左对齐、元数据右对齐制造张力。
禁止：渐变按钮、背景模糊光晕、统一大圆角、通篇居中。

## 双通道生效

- **常驻通道**：`profile/expression-profile.md`，Top N 高置信规则。每次会话读一次。
- **长尾通道**：`cases/cases.jsonl`，单条案例也在这里，按话题现查：

```bash
python "<skill-dir>/su.py" recall --intent "<当前任务一句话描述>"
```

两个通道**内容不重叠**：常驻规则在会话开始读一次 `profile` 就够了，`recall` 只给你长尾命中。
不想读两次就不要 `--with-core`，否则上下文翻倍。

```bash
python "<skill-dir>/su.py" recall --intent "<当前任务一句话描述>"
```

** `--intent` 写你这次要干的事，不是复述用户原话。** 没命中就按 L1 复述确认。

规则升级（同 rule 累计 >=2 份证据 → 进常驻）：

```bash
python "<skill-dir>/su.py" promote
```

## 目录

```
semantic-understanding/
├── SKILL.md                      本文件
├── su.py                         存储 / 检索 CLI + 判据 + 注入门控（零依赖）
├── hook.py                       UserPromptSubmit hook：每轮自动注入命中的习惯
├── viz.py                        面板渲染器（零依赖，含 UI 红线）
├── parity.py                     跨语言对拍：面板 JS 与 su.py 判据是否等价（零依赖）
├── config.json                   阈值与门控（min_score / tail_ratio / 短句兜底 / auto_viz）
├── profile/expression-profile.md 常驻档案（生成区 + 手动保护区）
├── cases/cases.jsonl             已确认案例（长尾；本地生长，不进仓库）
├── cases/cases.example.jsonl     示例数据（格式参考，随仓库分发）
├── pending/candidates.jsonl      待确认候选
├── .session-state/               hook 会话状态（自动生成，7 天清理）
└── viz/index.html                可视化面板（自动生成，自包含）

仓库另含 `README.md` / `LICENSE` / `.gitignore`。个人数据层（`cases.jsonl` /
`candidates.jsonl` / `expression-profile.md`）由 `.gitignore` 挡住，不进公开仓库。
```

`<skill-dir>` = 本技能所在目录（如 `~/.workbuddy/skills/semantic-understanding`）

## 命令速查

```bash
SU="<skill-dir>/su.py"

python "$SU" capture --surface "<原话>" --intended "<真意>" \
        --literal "<我理解成了啥>" --correction "<他怎么改的>" \
        --rule "<可泛化规则>" --domain "<领域>" --tags "a,b"

python "$SU" pending                       # 待确认清单
python "$SU" confirm <id>                  # 确认入库
python "$SU" reject <id>                   # 丢弃
python "$SU" recall --intent "<话题>"      # 长尾检索 → 注入块
python "$SU" promote                       # 升级常驻规则
python "$SU" profile                       # 看常驻档案
python "$SU" stats                         # 概览
python "$SU" viz                           # 重绘可视化面板（写操作已自动触发）
python "$SU" viz --open                    # 重绘并打开浏览器
python "$SU" pin <case-id>                 # 置顶：无视证据阈值，强制进常驻档案
python "$SU" unpin <case-id>               # 取消置顶
python "$SU" forget <id>                   # 退役过时/记错的规则
python "$SU" doctor                        # 体检常驻机制（hook 是否挂上、解释器路径是否漂移）

SKILL="<skill-dir>"
python "$SKILL/parity.py"                  # 跨语言判据对拍：改了 su.py 判据后必跑
```

### 什么时候用 `pin`

默认 `promote_threshold=2`，即同一条 rule 要被 **2 个独立案例**支撑才自动升级为常驻。
这个阈值是防噪用的：对话里的单次误解可能是偶发口误，不应立刻变成"每次都在影响解读"的规则。

但两条情况值得手动置顶：

1. **用户明确宣告过的规矩**（"以后都这样"、"记住这一点"）—— 一次就够，直接 `pin`。
2. **代价极高的误解**（做错要炸的那类）—— 等第二次证据不划算。

置顶后跑 `promote` 生效，`unpin` 可撤销。

## 成本纪律：调用重型技能前的自检（2026-09-25 立）

> **起因**：一次重型技能的补办登记，烧掉了整条会话里最贵的一笔开销 —— 其中相当一部分
> 来自可避免的返工（同一份文档分几次读、档位选错重来、被写入闸门拦下后再补课）。
> 成本对使用者是硬约束：贵到用不起，能力就等于不存在。这不是偏好问题，是可行性问题。
>
> **底层公式**：积分 ≈ 新进入上下文的字符量 × 模型往返次数。
> 返工受双重惩罚：既重复了内容，又多付了往返。

**适用范围**：多文件登记类、流程编排类、批量写入类任务之前
（project-registry、工厂类流程、多份文档产出）。轻量单文件改动不适用，别为它加戏。

### 一、动手前 —— 这三条消灭大多数返工

| 动作 | 为什么 | 真实反面案例 |
|---|---|---|
| **一次读全，别边做边读** | 同一份文档分次读，每段都独立进历史 | 同一份 SKILL.md 分 5 次读 |
| **受约束字段先查合法枚举** | 写错一次 = 整批重做 | `--status 在用` 不存在，7 项资产中 3 项重写 |
| **规模 / 档位先定再 init** | 换档要整个重来 | 先跑 L 档，发现要独立决策目录又升 M 档重来 |

### 二、写入时

| 动作 | 为什么 | 真实反面案例 |
|---|---|---|
| **决策卡禁止前向引用未发号的 ID** | 会污染发号游标，后续 ID 全部错位 | ADR-001 写了「关联 SKILL-001」，首个技能被发成 SKILL-002 |
| **同位置多条目，先想清要不要 `--force`** | 被闸门拦下 = 补一张说明卡 + 重来 | 3 个职责共用 su.py，被查重拦下后才补 ADR-006 |
| **独立命令合并成一条**（同文件多次写仍必须串行） | N 次往返 = N × 当前历史长度 | 7 次 `add` 拆成 4 批 |
| **长内容写短，能外置就外置** | 全文进历史，是单块最大体积来源 | 6 张 ADR 全文 |

### 三、节奏 —— 唯一能压掉「天然贵」的杠杆

- **一个阶段做完就收工，换新会话。** 历史归零后成本回到起点；在一条会话里滚十几轮，
  单轮入参随轮数线性上涨，累计是平方级。
- **不是为了省而拆碎**：把一轮拆成三轮并不省（历史照样累积），**换会话才省**。
- **不为验证而重复验证**：同一件事跑过一遍就记结论，别为了"确认一下"再跑。

### 反模式自检（出现任一，立即停下重新规划）

1. 同一个文件今天读第二遍
2. 同一批命令分两批发
3. 被闸门拒绝后立刻重试，而不是先读清它的要求
4. 写完才发现枚举值 / 依赖 ID 不对
5. 为了"确认一下"重跑刚才跑过的验证

> **不扩张边界**：这一章**只写进 SKILL.md，不写进 hook 注入的常驻规则**。
> 常驻规则每轮都注入，把成本纪律塞进去 = 它自己变成每轮固定成本。
> 同理，本技能的 `description` 不因此加字 —— 它同样每轮注入。

## 红线

1. **不存敏感物**：密码、token、密钥、他人隐私细节。这是表达档案，不是黑箱日记。
2. **不许静默写成 facts**：候选必须过用户确认。`Rule` 进了常驻档案就变成了"每次都在影响解读"的东西，代价高于收益时宁可不留。
3. **规则不等于免死金牌**：L0 命中的不可逆动作，仍然要在动手前复述（见 L1 第 3 条）。
4. **定期清理**：用户说"这条作废了/现在不一样了" → `forget`，然后 `promote` 同步。人表达习惯会变，档案跟着变。
5. **不许为了凑数制造候选**：一次会话没有误解就是没有，别硬提炼。
6. **不改其他技能**：本层只读/写自身目录，对 alchemy、ae-factory 及其他技能零侵入。
